name: Keep Supabase alive

on:
  schedule:
    - cron: '0 12 */3 * *'   # a cada 3 dias, evita a pausa por inatividade de 7 dias do plano free
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API (orçamento CPZ/BA Vision)
        run: |
          curl -sf "https://eqbmqqfsbooatsmzgttz.supabase.co/rest/v1/propostas?select=id&limit=1" \
            -H "apikey: sb_publishable_IDA-WMWDbUE4cj0LllUeHg_Epoj7oa5" \
            -H "Authorization: Bearer sb_publishable_IDA-WMWDbUE4cj0LllUeHg_Epoj7oa5"
